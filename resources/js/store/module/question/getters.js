export default {
    getQuestion(state) {
        return state.questions
    },

    getTotal(state) {
        return state.total
    },

    getLastPage(state) {
        return state.last_page
    }
}
