export default {
    getCategory(state) {
        return state.categories
    },

    getTotal(state) {
        return state.total
    },

    getLastPage(state) {
        return state.last_page
    }
}
